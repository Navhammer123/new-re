package newpack;

import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;

public class Googlepage {

	public static void main(String[] args) {
		//ChromeOptions options = new ChromeOptions();
		//options.addArguments("headless");
		//WebDriver driver = new ChromeDriver();
		//driver.manage().window().maximize();
		//Actions actions = new Actions (driver);
		//driver.get("https://demo.opencart.com/");
		//WebElement srcbox = driver.findElement(By.name("SEARCH"));
		
		//WebElement menu = driver.findElement(By.cssSelector("ul.nav.navbar-nav>li:nth-child(3)"));
		
		//WebElement item = driver.findElement(By.xpath("//ul[@class='list-unstyled']//a[contains(text(),'Monitors (2)')]"));
		
		//actions.moveToElement(menu).click(item).build().perform();
		//driver.get("https://www.stqatools.com/demo/DoubleClick.php");
		
		//WebElement btn = driver.findElement(By.xpath("//button[text = 'Click Me /Double Click Me!']"));
		
		
		//driver.get("https://automationbookstore.dev/");
		//driver.findElement(By.cssSelector("input[data-type='search']")).sendKeys("java");
		//WebElement close = driver.findElement(By.cssSelector("a[title='Clear text']"));
		
		//System.out.println(close.isDisplayed());
		//if(close.isDisplayed()) {
		//	close.click();
		//}
		
		
		
//		System.out.println("page title .." + driver.getTitle());
		//System.out.println ("new url :" + driver.getCurrentUrl());
       //WebElement search = driver.findElement(By.id("APjFqb"));
		//WebElement search = driver.findElement(By.className("gLFyf"));
		//WebElement search = driver.findElement(By.tagName("textarea"));
		
		//search.sendKeys("cricket");
		//search.sendKeys(Keys.ENTER);
		
		//driver.navigate().back();
		
		
		
		

	}

}
